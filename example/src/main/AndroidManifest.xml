<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mteam.movie_trailer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.mteam.movie_trailer.WatchlistApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">
        <activity android:name="com.mteam.movie_trailer.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.mteam.movie_trailer.activity.PipActivity"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:supportsPictureInPicture="true"
            android:theme="@style/NoActionBar" />
        <activity android:name="com.mteam.movie_trailer.fragment.login.LoginActivity" />
        <activity
            android:name=".debug.DebugActivity"
            android:theme="@style/Theme.AppTest" />
    </application>

</manifest>